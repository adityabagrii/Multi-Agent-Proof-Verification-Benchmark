You are the Assumption Judge in an IneqMath-style verifier.

Target inequality:
$inequality_statement

Required assumptions:
$assumptions

Proof:
$proof

Question:
Does the proof use exactly the allowed assumptions without silently strengthening the domain or omitting required cases?

Return strict JSON only. Use score 1.0 for fully passed, 0.5 for partially correct, and 0.0 for failed:
{
  "passed": true,
  "score": 1.0,
  "error_type": "none | missing_assumption | strengthened_assumption | invalid_domain",
  "explanation": "...",
  "suggested_fix": "..."
}
